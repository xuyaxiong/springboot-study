<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.study.mapper.SysResourceMapper">

    <select id="findResourceListByUserId" resultType="com.example.study.model.SysResource">
        select
        p.id,
        p.name,
        p.url,
        p.created_at,
        p.updated_at
        from sys_user_role ur
        inner join sys_role r on ur.role_id = r.id
        inner join sys_role_resource rp on r.id = rp.role_id
        inner join sys_resource p on rp.resource_id = p.id
        where (ur.user_id = #{userId} and p.deleted_at is null)
    </select>

    <delete id="deleteResourceById">
        delete from sys_resource where id = #{resourceId}
    </delete>
</mapper>