<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.study.dao.UserDao">
    <insert id="insert" parameterType="com.example.study.model.User">
        insert into user (name, email) values (#{name}, #{email})
    </insert>
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from user where id = #{id}
    </delete>
    <select id="findAll" resultType="com.example.study.model.User">
        select * from user
    </select>
    <select id="findById" resultType="com.example.study.model.User" parameterType="java.lang.Integer">
        select * from user where id = #{id}
    </select>
</mapper>