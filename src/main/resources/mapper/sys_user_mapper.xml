<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.study.mapper.SysUserMapper">

    <select id="findUserByUsername" resultType="com.example.study.model.SysUser">
        select
        id,
        username,
        password,
        account_non_expired,
        account_non_locked,
        credentials_non_expired,
        enabled
        from sys_user
        where username = #{username}
    </select>

    <insert id="addUser" parameterType="com.example.study.model.SysUser">
        insert into sys_user(
        username,
        password,
        account_non_expired,
        account_non_locked,
        credentials_non_expired,
        enabled
        ) values (
        #{username},
        #{password},
        #{accountNonExpired},
        #{accountNonLocked},
        #{credentialsNonExpired},
        #{enabled}
        )
    </insert>
</mapper>