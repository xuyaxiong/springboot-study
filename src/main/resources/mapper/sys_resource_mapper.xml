<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.study.mapper.SysResourceMapper">

    <select id="getSysResourceListByUserId" resultType="com.example.study.model.SysResource">
        select
        p.id,
        p.name,
        p.url
        from sys_user_role ur
        left join sys_role r on ur.role_id = r.id
        left join sys_role_resource rp on r.id = rp.role_id
        left join sys_resource p on rp.resource_id = p.id
        where ur.user_id = #{userId}
    </select>
</mapper>